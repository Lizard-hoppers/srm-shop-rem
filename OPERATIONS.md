# OPERATIONS — Electronics CRM

Сверяться с этим файлом в начале и в конце каждой рабочей сессии (правило
Clean & Live Bot Manifest, п.10).

## Что это

CRM для магазина электроники + мастерской по ремонту: клиенты, склад по
ячейкам, ремонты с привязкой к мастеру, приход комплектующих от
поставщиков, продажи (онлайн/офлайн). Весь интерфейс персонала — единое
Telegram Mini App (не отдельная веб-панель + отдельный бот, как было в
исходном плане — решение поменялось по ходу разработки). Общая
SQLite-база (WAL).

**Вход:** только по Telegram ID, без пароля (кроме самого первого раза для
нового сотрудника — см. «Онбординг нового сотрудника» ниже). Сессия не на
cookie (Telegram-вебвью ненадёжно их сохраняет между запросами), а на
подписанном токене `?t=...`, который прокидывается через каждую
ссылку/форму/редирект (`webapp/deps.link()` и `webapp/templating.render()`).

**Роли (RBAC):** `owner`/`admin`/`master`/`storekeeper` в колонке
`staff.role`, проверяются через `webapp/deps.require_role(*roles)` (поверх
`require_staff`). `/reports` — только owner+admin; мутации склада
(`/inventory/products`, `/cells`, `/movements/*`) — owner+admin+storekeeper;
мутации ремонтов (создание/статус/назначение/цена/детали) —
owner+admin+master. Просмотр списков (GET) открыт всем залогиненным. До
18.08 ролей не было вообще — любой залогиненный видел выручку и мог
списывать склад, см. commit `2c0d491`.

## Статус по фазам (план: `.claude/plans/fancy-greeting-pearl.md` у Павла)

- [x] Фаза 0 — каркас: схема БД (`core/storage.py`), staff-аккаунты,
      `simulate_tests.py`.
- [x] Фаза 1 — Клиенты и склад: CRUD клиентов, товары, ячейки, движения
      склада (приход/списание/перемещение), отчёт по низким остаткам.
- [x] Фаза 2 — Ремонты: приём устройства, назначение мастера,
      статус-пайплайн (новый→в работе→готов→выдан), списание деталей на
      заказ, цена/гарантия, история статусов.
- [x] Фаза 3 — Приход комплектующих: поставщики, накладные с несколькими
      позициями, авто-списание на склад.
- [x] Фаза 4 — Продажи офлайн (POS-лайт): позиции с ценой, авто-подбор
      ячейки списания, чек.
- [x] Фаза 6 — Отчётность: ремонты по статусам/мастерам, среднее время
      ремонта, продажи по каналам, дефицит склада.
- [x] Telegram Mini App вместо отдельного бота+веб-панели: вход по
      Telegram ID (`core/telegram_auth.py`, `core/session_token.py`,
      `webapp/routers/miniapp.py`), тёмная тема (`webapp/static/style.css`).
- [x] Клиентские QR-карты лояльности (первый кусок Фазы 5): клиент
      сам регистрируется в боте, поделившись номером телефона
      (`bot/handlers.py`), получает QR-фото (`CRMCID:<id>`,
      `core/qr.py`). Персонал сканирует QR нативным сканером Telegram
      прямо в разделе «Клиенты» (`Telegram.WebApp.showScanQrPopup`) —
      сразу открывается карточка. QR-картинка также видна в самой
      карточке клиента (`/clients/{id}/qr.png`).
- [ ] Остальная Фаза 5 — статус ремонта и каталог для клиента в боте
      (сейчас бот клиенту только регистрирует и присылает QR).
- [ ] Продажи online-канала (кнопки в UI есть — `channel=online`, но нет
      отдельного клиентского входа, который бы их создавал).
- [ ] Скидки по карте лояльности не считаются автоматически — id карты
      просто ускоряет поиск клиента, логику скидок ещё не обсуждали.

## Уведомления в рабочую группу

Рабочая группа — Telegram-форум «Работа» (`chat_id=-1004478202416`, бот в
ней админ с `can_manage_topics`), с отдельными темами под разные потоки
заказов:

| Тема | thread_id | Статус |
|---|---|---|
| Ремонт техники | 5 | подключена — карточка на каждый новый ремонт |
| Заказы офлайн (клиент заказывает то, чего нет на складе, под заказ у поставщика) | 3 | информационная, ничего не постит пока — нет отдельной сущности "заказ под клиента" в схеме |
| Онлайн заказы: Olx, Prom | 12 | информационная — задел под будущую интеграцию с OLX/Prom.ua (синхронизация остатков) |

Отдельно есть плоская группа «Мастера 007» (`chat_id=-1003820178638`, без
тем, бот там тоже админ) — общий чат всех мастеров по ремонту (пока один на
всех, без разделения по мастерам).

При приёме устройства в ремонт (`POST /repairs`) `crm_web` шлёт карточку
(клиент, телефон, устройство, неисправность, мастер, оценка, канал)
напрямую в Telegram Bot API (`core/notify.py`, `httpx` →
`api.telegram.org`, без похода через процесс `crm_bot`) в **оба места
сразу**: тему «Ремонт техники» и группу «Мастера 007»
(`notify_repair_card()` → `CRM_STAFF_GROUP_CHAT_ID`+`CRM_REPAIR_TOPIC_ID=5`
и `CRM_MASTERS_GROUP_CHAT_ID`). Каждое из трёх назначений (`_send()` в
`core/notify.py`) независимо: если конкретная переменная не задана — тот
конкретный пункт молча пропускается, остальные всё равно уходят; ошибка
Telegram API тоже только логируется, запрос не падает.

**Кнопки на карточке (18.08).** Карточка не статичная — под ней кнопки,
которые ведут ремонт по статус-пайплайну прямо из Telegram, без захода в
приложение:
- `new` → «🔧 Взять в работу» (`repair_take:{id}`) — атомарно
  (`core/repairs.claim_repair()`, `UPDATE ... WHERE status='new'` +
  проверка `rowcount`, а не слепой `UPDATE` — вторая одновременная попытка
  просто проигрывает гонку, а не тихо перезаписывает первую). Если на
  приёме уже назначили конкретного мастера — кнопка реально сработает
  только для него (или admin/owner), остальные получат алерт «Закреплено
  за …».
- `in_progress` → «✅ Готово» (`repair_done`, только для взявшего мастера
  или admin/owner с `override=True`) и «↩️ Отменить» (`repair_release`,
  возврат в `new` без мастера — на случай, если взяли по ошибке).
- `ready`/`issued`/`cancelled` → кнопок нет.

Карточка **редактируется на месте** (`core/notify.edit_message()` /
`sync_repair_cards()`), а не дублируется новым сообщением — какое именно
сообщение редактировать, знает новая таблица `repair_order_messages`
(`chat_id`+`message_id` на каждый отправленный адресат). Синхронизация
двусторонняя: ручная смена статуса из приложения (`POST
/repairs/{id}/status`) тоже дёргает `sync_repair_cards()` — кнопка в
Telegram и статус в приложении никогда не расходятся, старая карточка не
может позволить нажать «Готово» на том, что уже закрыли из приложения.

Обработчики кнопок — `bot/repair_actions.py` (три `callback_query`
хендлера, зарегистрированы в `bot/bot.py`), сотрудник определяется по
`callback.from_user.id` через `core.auth.get_staff_by_telegram_id()` —
тот же путь, что и вход в веб-панель.

**Как узнать chat_id/thread_id новой темы:** добавить бота в группу как
админа → открыть тему → скопировать номер из ссылки `t.me/c/<internal_id>/<thread_id>`
(chat_id = `-100<internal_id>`), либо отправить `/chatid` в общем чате
группы (не в теме) — бот ответит числом chat_id самой группы. thread_id
темы командой не достать, только из ссылки на сообщение в этой теме.

**Дальше по плану (не реализовано):** разбить общую группу «Мастера 007»
на подгруппу под каждого выбранного мастера — обсудить структуру, когда
владелец определится с реальным составом мастеров (сейчас в `staff` только
3 заглушки: Мастер 1/2/3).

## Быстрый приход товаров (план `.claude/plans/gentle-cuddling-steele.md`)

Три уровня по нарастанию сложности, чтобы вбивать накладную от поставщика
было максимально быстро (`webapp/routers/purchases.py`,
`webapp/templates/purchases_list.html`):

- **Уровень 1 (18.08, commit `1b33f23`) — сделано.** Строки формы
  динамические («+ Добавить позицию», не фикс. 3), товар ищется по
  названию/SKU через `<datalist>` (тот же `| tojson`-паттерн, что и
  каталог устройств в приёме ремонта — не `| safe`), ячейка подставляется
  сама по товару (`core/inventory.default_cell_by_product()` — где сейчас
  лежит больше всего этого товара). Если товара нет в каталоге — строка
  помечается «🆕 новый товар», на сабмите создаётся автоматически
  (`core_inventory.create_product()` с дефолтами, донастроить
  категорию/цену можно потом на «Товары»). Заодно закрыт пробел в RBAC —
  роуты прихода раньше не проверяли роль вообще, теперь
  owner/admin/storekeeper (`_PURCHASE_ROLES` в роутере).
- **Уровень 2 (18.08, commit `2cde00a`) — сделано.** Textarea «Вставить
  накладную» + кнопка «Разобрать» — вставляете список из Excel/таблицы
  поставщика, `core/purchase_import.parse_invoice_text()` разбирает
  построчно (таб/`;`/2+ пробела — разделители колонок) и сопоставляет с
  каталогом (точный SKU → точное имя → подстрока). Несопоставленное — тот
  же «🆕 новый товар» флоу из Уровня 1. Только предлагает разбор, в БД
  ничего не пишет, пока не нажали «Оприходовать».
- **Уровень 3 (18.08, commit `6a69b29`) — сделано.** Сотрудник
  (owner/admin/storekeeper) шлёт боту в личку фото бумажной накладной —
  без команды, просто фото (`bot/purchase_photo.py`,
  `@router.message(F.photo)`). `core/vision_ocr.py` шлёт фото в OpenAI
  vision (`httpx`, модель по умолчанию `gpt-4o-mini`, переопределяется
  `OPENAI_VISION_MODEL`), возвращает позиции; `core/purchase_import.match_items()`
  сопоставляет с каталогом (тот же матчер, что и в Уровне 2). Результат —
  черновик в `purchase_drafts` (`status='pending'`), бот присылает превью +
  две кнопки: «✅ Оприходовать как есть» срабатывает, только если
  **у каждой позиции есть и количество, и подходящая ячейка** (у нового
  товара ячейки взяться неоткуда — фото не может знать, куда физически
  положить деталь) — иначе всё-или-ничего отказ с подсказкой на вторую
  кнопку; «✏️ Открыть и поправить» — настоящая WebApp-кнопка (личка с
  ботом, не группа) на `/purchases/draft/{id}`, та же форма из Уровня 1
  (переиспользует `webapp/static/purchase-rows.js`, вынесенный туда из
  инлайна ради этого переиспользования), предзаполненная черновиком.
  Ошибка распознавания/недоступность OpenAI — `VisionOcrError`, бот
  отвечает «не смог распознать», ничего не падает и не пишется в склад.
  Проверено вживую реальным вызовом OpenAI API (синтетическая тестовая
  накладная через PIL) — распознавание, сопоставление, черновик, страница
  правки, сабмит — весь путь пройден на проде, тестовые записи вычищены.
  **`OPENAI_API_KEY` — тот же, что попал прямым текстом в чат при выдаче
  18.08; Павел сообщил, что перевыпустил на platform.openai.com, но
  значение в `.env` на момент коммита `6a69b29` всё ещё успешно
  аутентифицировалось — стоит перепроверить, что ротация реально
  применилась на стороне OpenAI.**
- **Сканирование камерой прямо в Mini App (18.08, commit `f9e87b1`) —
  сделано.** Кнопка «📷 Сканировать накладную» на `/purchases` открывает
  камеру устройства напрямую (`<input type="file" capture="environment">`,
  обычный HTML, работает в Telegram WebView без спец-API) — без похода в
  чат с ботом. `POST /purchases/scan` гоняет фото через тот же
  `vision_ocr`+`match_items()` пайплайн, что и бот, возвращает тот же
  формат, что `/purchases/parse` — строки заполняются на той же странице
  синхронно, без черновика в `purchase_drafts` (форма и есть шаг проверки).
  Проверено вживую реальным вызовом OpenAI API. 136/136 тестов.

## Карточка товара (18.08, commit `aa6af0a`)

Строки в «Склад → Товары» кликабельны → `/inventory/products/{id}` —
просмотр (все роли), форма редактирования и загрузка фото (owner/admin/
storekeeper, `_STOCK_WRITE_ROLES`, как и другие складские мутации в
`webapp/routers/inventory.py`). Фото — файл на диске
`webapp/static/product_photos/{product_id}_{uuid4}.{ext}` (свой filename
генерируется всегда, никогда не берётся у клиента — от path traversal),
content-type валидируется по allowlist (jpeg/png/webp) на сервере, старое
фото удаляется при замене. Директория в `.gitignore` (runtime-данные, не
исходники) — на новом сервере создаётся автоматически при первой загрузке
(`os.makedirs(..., exist_ok=True)`). `products.photo_path` — новая
колонка через `_ensure_column()` (таблица уже с боевыми данными на
проде, не свежий `CREATE TABLE`). 146/146 тестов. Проверено вживую:
реальная карточка (id=7) открылась, тестовый товар отредактирован и
фото реально легло на диск + отдалось по `/static/product_photos/...`.

**Баг и фикс 18.08 (commit `7d7fdba`): загрузка фото товара «вешала»
мини-приложение.** Причина — в `/etc/nginx/sites-available/crm-miniapp`
не было `client_max_body_size`, действовал дефолт nginx 1 МБ; реальное
фото с камеры — обычно 2-8 МБ. Форма загрузки была обычным `<form
enctype="multipart/form-data">` (нативная навигация браузера, не AJAX) —
когда nginx резал запрос, странице некуда было вывести ошибку, и
Telegram WebView просто зависал на пустом экране в процессе навигации
(сам Telegram не падал). **Важно помнить:** у этого домена nginx
настроен без `client_max_body_size` по умолчанию — если добавляется
новый эндпоинт с загрузкой файлов, лимит уже поднят до 20M на весь
`location /` (общий для всего сайта), но если появится домен/сайт с
нуля — не забыть про этот же нюанс.
Фикс: `client_max_body_size 20M;` в nginx (бэкап старого конфига рядом,
`nginx -t` + reload) + оба фото-эндпоинта (`/inventory/products/{id}/photo`,
`/purchases/scan`) теперь режут чтение на 15 МБ сами и отдают
дружелюбный JSON 413 вместо чтения без лимита; форма загрузки фото товара
переведена на `fetch`+`FormData` с индикатором «Загружаю…» и текстом
ошибки вместо голой навигации. 149/149 тестов. Проверено вживую через
реальный HTTPS-домен (не localhost) синтетическим ~4 МБ фото — раньше
упёрлось бы в старый лимит 1 МБ, теперь 200 OK меньше чем за секунду;
25 МБ файл (выше нового потолка) отклоняется чисто 413, сервис не падает.

**Сканер штрихкода/этикетки для SKU (18.08, commit `4da0838`).** Кнопка
📷 справа от поля SKU — и на форме «Добавить товар», и в карточке
редактирования. Фото → `core/vision_ocr.extract_product_label()` (тот же
OpenAI vision, другой промпт) → JSON `{name, sku}`, автозаполняет оба
поля на форме. Цена НИКОГДА не возвращается и не автозаполняется —
осознанно, склад в основном китайские детали, printed/barcode цена не
совпадает с местной и будет только сбивать с толку. Тот же лимит 15 МБ +
дружелюбная ошибка, что и у других двух фото-эндпоинтов
(`webapp/static/product-scan.js` — общий JS, кнопка ищется по классу
`scan-label-btn` + `data-name-target`/`data-sku-target`). 154/154 тестов.
Проверено вживую реальным вызовом OpenAI — синтетическая этикетка с
названием+кодом+фейковым штрихкодом, модель верно вытащила и перевела
название, выбрала осмысленный код вместо голых цифр штрихкода, цены в
ответе нет.

**Карточки товаров вместо таблицы (18.08, commit `642f53d`).** «Склад →
Товары» — сетка карточек `.cards`/`.card` (тот же паттерн, что на
дашборде/хабах), 2 колонки, маленькое квадратное фото сверху (или
плейсхолдер-иконка), название, SKU+категория, «Остаток: N шт (мин. M)».
Заодно разобрался с жалобой «показывает 05 вместо 5»: это была не одна
проблема, а две — (1) у товара #7 поле «Единица» реально содержало текст
`"5"` вместо `"шт"` (опечатка при вводе, не баг кода), старая таблица
склеивала `{{ qty }} {{ unit }}` без подписи → «0 5» читалось как «05»,
поправлено в БД напрямую; (2) поле «Мин. остаток» на форме создания имело
`value="0"` (не placeholder) — на мобильном при тапе в поле с уже
непустым «0» и допечатывании цифры легко получить «05» (для Python
парсинга не страшно, `int("05")==5`, но именно так, похоже, разъехались
поля при вводе) — заменено на `placeholder="0"`, дефолт 0 при пустом
поле остаётся через `optional_int(...) or 0` на сервере. 154/154 тестов
(чистый UI/CSS, тесты не менялись).

**Чекбоксы товара — правились дважды (18.08, commit `53dcf30`,
`15ba414`).** Сначала кликабельность (`form.edit-form label` ловило и
голые чекбокс-лейблы, разваливая квадратик и текст на два ряда — добавлен
класс `.checkbox-label` с `flex-direction: row`), потом по просьбе Павла
— крупнее (24px) и в один ряд (`.checkbox-row`), кликабельна вся плашка
с рамкой, не только квадратик. **Важно:** правки `style.css` без
`systemctl restart crm_web` не гарантированно видны — `asset_version`
(кэш-бастинг `?v=` у статики) берётся от времени старта процесса, не от
содержимого файла, так что уже закэшированная в Telegram WebView копия
может не обновиться по тому же URL, пока процесс не перезапущен.

**«Единица» — выпадающий список, не текст (18.08, commit `5586b79`).**
Баг «5 попадает в поле Единица» (см. выше, товар #7) повторился ЕЩЁ РАЗ
после первого ручного фикса данных — оказалось, это не разовая опечатка,
а системная уязвимость: текстовое поле рядом с числовым легко словить
случайный ввод на мобильном. Убран `<input type="text">`, теперь
`<select>` (шт/уп./компл./м/пара, «шт» по умолчанию) — вписать туда
цифру физически нельзя. На форме редактирования: если у товара уже
сохранено значение единицы не из этого списка (легаси-данные), оно
добавляется отдельным `selected`-option с точным текущим значением —
сохранение других полей карточки никогда не должно тихо подменять
единицу на «шт» как побочный эффект.

**«Единица» — снова переделано, теперь без `<select>` (18.08, commit
`d76294f`).** Даже выпадающий список не спас — Павел сообщил, что после
выбора значения выскакивало ВТОРОЕ «прозрачное телеграмовское меню» с
повторным выбором. Похоже, Telegram WebView сам перехватывает нативный
`<select>` под свой picker независимо от нашего HTML/CSS (та же природа
проблемы, что описана в шапке `select-enhance.js` про другие вебвью —
просто здесь бьёт иначе). Убрал `<select>` вообще, поставил тот же
`<input list="unitList">` + `<datalist>`, что уже проверенно работает в
поиске товара на «Приходе» и в каталоге устройств при приёме ремонта —
обычное текстовое поле с подсказками, а не модальный picker, так что
телеграму нечего перехватывать. Ячейка на новой форме «Добавить остаток»
(ниже) осталась настоящим `<select>` — на неё жалоб не было, менять её
заодно не было причины.

**Добавить остаток прямо с карточки товара (18.08, commit `d76294f`).**
Второй репорт Павла — «некуда вписать сколько товара осталось». По
архитектуре остаток считается по ячейкам через леджер
`core.inventory.record_movement()` (manifest п.11 — в обход леджера
списывать/приходовать нельзя), поэтому прямого числового поля на самом
товаре никогда не будет. Вместо этого — мини-форма «Добавить остаток» на
карточке товара (`POST /inventory/products/{id}/receive`, ячейка+кол-во,
дергает тот же `receive_stock()`, что и полноценный «Приход»). Заодно
создание нового товара теперь сразу редиректит на его карточку (было — в
общий список), чтобы сразу было куда добавить остаток, не разыскивая
свежесозданный товар в списке. 158/158 тестов, проверено вживую полным
циклом (создал → попал на карточку → добавил остаток → леджер реально
сдвинулся).

**Чекбоксы — третий и (похоже) финальный заход (18.08, commit
`2198595`).** Проверил напрямую сервер — и сохранение, и рендер текущего
состояния были верны на 100%. Причина оказалась в мобильном
Telegram-клиенте конкретно (десктоп работал нормально) — известный баг
мобильного WebKit/Chrome: `<label>` со `display:flex`, неявно
оборачивающий `<input type="checkbox">`, ненадёжно передаёт тап в toggle
на некоторых мобильных движках рендеринга, хотя та же разметка отлично
работает на десктопном движке Telegram. Убрал `display:flex` с самого
`<label class="checkbox-label">` (теперь `display:block`, раскладка
чекбокса+текста через `vertical-align`+`margin`, не flex) + добавил
JS-подстраховку в `webapp/static/product-scan.js` — клик по любой точке
лейбла, кроме самого чекбокса, тоглит его вручную и шлёт `change`;
клик прямо по чекбоксу — нативное поведение, двойного срабатывания нет.
**Урок на будущее:** при жалобах на кликабельность/интерактивность в
Mini App сразу уточнять «мобильный или десктоп Telegram» — многие
CSS/JS-баги в этом проекте воспроизводятся только на одном движке.

## Бот реагировал на фото/контакты в группах, не только в личке (18.08, commit `67dea42`)

Баг: фото запчасти и контакт клиента, отправленные в теме «Обсуждение»
группы «Работа», бот принял за накладную на распознавание и за попытку
регистрации клиента. Причина — `bot/purchase_photo.py`
(`@router.message(F.photo)`) и `bot/handlers.py` (`CommandStart()`,
`F.contact`) писались и тестировались только под личку с ботом, без
фильтра по типу чата. Как только бот стал участником групп «Работа» и
«Мастера 007» (ради карточек ремонта) — эти хендлеры начали ловить ЛЮБОЕ
фото/контакт в любой теме этих групп. Фикс: у всех трёх добавлен
магик-фильтр `F.chat.type == "private"` (проверено напрямую —
сконструировал реальные `Message` с `Chat(type='private')` и
`Chat(type='supergroup')`, фильтр корректно различает). `Command("chatid")`
специально НЕ ограничен — он для того и нужен, чтобы работать в группах.
Автотестов на диспетчер бота нет (известный пробел, см. аудит 18.08),
проверено рестартом + чистыми логами polling.

**Реализовано следующим шагом (18.08, commit `a81e338`, план
`.claude/plans/gentle-cuddling-steele.md`):**

- **Контакт от сотрудника → клиент.** `got_contact()` в
  `bot/handlers.py` теперь различает: свой контакт в личке — как и
  раньше, самостоятельная регистрация клиента; чужой контакт от
  сотрудника (группа ИЛИ личка) — кнопка «➕ Добавить как клиента»
  (`offer_add_client`/`confirm_add_client`), контакт достаётся обратно
  через `callback.message.reply_to_message` (Telegram хранит цепочку
  reply), клиент создаётся через уже существующий
  `core.clients.get_or_create_by_phone()` — тот же путь, что у веб-формы
  и приёма ремонта, дублей по телефону не будет.
- **Фото-реплай на карточку ремонта → вложение в историю.** Новый
  `bot/repair_attachments.py`
  (`F.photo, F.reply_to_message, F.chat.type != "private"` — строго
  группа, специально исключена личка, чтобы не перехватывать
  reply-фото у `bot/purchase_photo.py`'s DM-only OCR-хендлера накладных
  раньше него). Триггер — ответ именно на карточку ремонта (резолвится
  через уже существующую `repair_order_messages`, ту же таблицу, что
  использует `sync_repair_cards()`), без reply — тихо игнорируется,
  никакого пикера «выберите ремонт». Новая таблица `repair_attachments`
  (`core/storage.py`), функции `find_order_by_message()`/
  `add_attachment()`/`get_attachments()` в `core/repairs.py`. Файлы —
  `webapp/static/repair_photos/` (тот же паттерн, что `product_photos/`,
  в `.gitignore`). Отображение — секция «📎 Вложения» на
  `webapp/templates/repair_detail.html`.

164/164 тестов (core-функции покрыты round-trip тестом, диспетчер бота —
по-прежнему нет, известный пробел). Проверено вживую полным циклом:
реальный ремонт → карточки реально ушли в оба чата → `find_order_by_message()`
резолвит оба обратно на repair → симуляция того, что делает хендлер
(скачать/сохранить/`add_attachment`) → видно на карточке ремонта, файл
отдаётся по `/static/`.

## Карточки ремонтов вместо таблицы + фото устройства (18.08, commit `cc85fea`)

«Ремонты» — та же сетка `.cards`/`.card`, что уже была у «Склад →
Товары»: фото устройства (или плейсхолдер-иконка) сверху, имя клиента,
устройство, пилюли статуса+канала в ряд, мастер и дата снизу. CSS-классы
переименованы `.product-card*` → `.thumb-card*` (общие теперь для обеих
сеток, не только товаров), добавлен `flex-wrap` на `.thumb-card-meta`
под две пилюли в ряд у ремонтов.

Фото — на карточке ремонта, секция вверху левой колонки, `POST
/repairs/{order_id}/photo` дословно копирует
`inventory.product_photo_view` (allowlist jpeg/png/webp, лимит 15 МБ,
удаление старого файла при замене, JSON-ответ). Фото привязано к
**устройству** (`devices.photo_path`, новая колонка через
`_ensure_column()`), не к конкретному заходу в ремонт — так оно
переживёт повторный ремонт того же устройства. Файлы —
`webapp/static/device_photos/{device_id}_{uuid4}.{ext}`, в
`.gitignore`.

Заодно вынес общий AJAX-скрипт загрузки фото в отдельный
`webapp/static/photo-upload.js` (driven by `data-photo-upload=` +
классы `.photo-preview`/`.no-photo-text`/`.photo-status`) — раньше
такой же код был инлайном только в карточке товара, при добавлении
второго места (карточка ремонта) дублировать его смысла не было;
`inventory_product_detail.html` тоже переведён на общий скрипт.

174/174 тестов (round-trip `set_device_photo` + HTTP-тесты
фото-эндпоинта: не-картинка, превышение размера, успешная загрузка
пишется в БД/на диск/рендерится на странице, плюс проверка что список
ремонтов — теперь `.cards`, не `<table>`). Проверено вживую на проде:
создал тестовый ремонт (он реально запостил карточки в «Работа» и
«Мастера 007» — это ожидаемо, тот же нотифай-путь, что у обычного
приёма), загрузил фото, проверил рендер на карточке и в списке, заменил
фото и убедился что старый файл удалился с диска, после — удалил
тестовые Telegram-сообщения через `deleteMessage` (id сохранены в
`repair_order_messages`), тестовые repair/device/client из БД и файл
фото с диска.

## Сканер серийника/IMEI на приёме ремонта (18.08, commit `2bad249`)

Кнопка 📷 справа от поля «Серийный №/IMEI» на форме приёма устройства —
по тому же принципу, что сканер SKU у товаров. `POST /repairs/scan-device`
(`core.vision_ocr.extract_device_info()`, тот же OpenAI vision) читает с
фото этикетку на коробке, гравировку/наклейку на корпусе устройства или
экран настроек «Об устройстве» и, если получилось, отдаёт СРАЗУ все
четыре поля — тип устройства, бренд, модель, серийный №/IMEI, а не
только серийник (так и просил Павел: «если он дает всю инфу по телефону
круто было бы если бы он заполнял это все»). Чего не разглядел на
фото — остаётся `null`, поле не трогается, дозаполняется руками. Как и
у сканера SKU, эндпоинт ничего не пишет в БД сам — ремонта на этот
момент ещё не существует, это чисто автозаполнение полей на клиенте.

**Важный нюанс промпта:** тип устройства обязательно приходит на
русском в каталожном формате («Смартфон», не «smartphone») — без этого
поле не совпадает с тем, что реально лежит в `device_catalog`
(`"Смартфон"`, `"Ноутбук"` и т.д.) и с даталистом подсказок на этом же
поле. Первая живая проверка вернула английское `"smartphone"` —
промпт ужесточён явным требованием («ОБЯЗАТЕЛЬНО на русском языке, с
большой буквы»), перепроверено — стало `"Смартфон"`.

**Рефакторинг заодно:** логика кнопки «сфоткал → распознал →
заполнил поля» была на грани дублирования второй раз (SKU-сканер уже
делал ровно то же самое, просто на два других поля и другой эндпоинт) —
вынесена в общий `webapp/static/scan-fill.js`
(`data-scan-endpoint`/`data-fields`-JSON/`data-status-target`), обе
формы товара тоже переведены на него; `product-scan.js` теперь содержит
только не связанную с этим JS-подстраховку тапа по чекбоксам.

177/177 тестов. Проверено вживую реальным вызовом OpenAI на
синтетической фотке (PIL: «Apple iPhone 13», «Model: A2482», «IMEI:
...», «S/N: ...») — все четыре поля распознались верно после фикса
языка типа устройства; серийник модель иногда путает 1-2 цифры на
синтетическом шрифте (ожидаемо для best-effort OCR, как и у сканера
SKU — человек проверяет перед сохранением, само поле никогда не
считается источником истины).

## Карточки клиентов вместо таблицы (18.08, commit `55bfc99`)

«Клиенты» — та же сетка `.cards`/`.thumb-card`, что уже у Товаров и
Ремонтов: плейсхолдер-иконка (у клиентов нет поля фото — используется
общая иконка `users`), имя, телефон (или «Без телефона»), пилюля
онлайн/офлайн, «В базе с …». Вся карточка — единая ссылка на
`/clients/{id}` (`<a class="card thumb-card">`, как у остальных двух
списков), а не `<div onclick=...>` со вложенной `<a href="tel:">` —
старая табличная реализация могла себе позволить вложенную ссылку
только потому, что навигация строки шла через JS `onclick` на `<tr>`,
а не через настоящий `<a>` (вложенные `<a>` — невалидный HTML, по-
разному ведут себя в разных движках). Звонок по номеру теперь на один
тап дальше — открыть карточку клиента, там `tel:`-ссылка уже есть.
179/179 тестов. Проверено вживую: страница отдаёт 200, рендерится
сетка карточек с пилюлями.

## Фильтр Все/Офлайн/Онлайн у клиентов (18.08, commit `93d53e9`)

Кнопки над сеткой карточек — тот же `.section-btns`, что у фильтра
статуса на «Ремонтах». `core.clients.list_clients()` получил
необязательный `source=` (комбинируется с существующим поиском по
имени/телефону через обычный AND, оба фильтра необязательны и
независимы). Поисковая форма хранит активный фильтр в скрытом поле
`source`, а кнопки фильтра хранят активный текст поиска (через
`urlencode`) — переключение одного не сбрасывает другой. 182/182
тестов (добавлены: фильтр реально исключает/включает нужных клиентов
по source, не просто рендерит кнопки). Проверено вживую: `/clients`,
`?source=online`, `?source=offline` — все 200.

## Продажи — форма чекаута переделана (18.08, commit `873b2f7`)

Раньше «Новая продажа» — жёстко 3 позиции (`ITEM_ROWS = 3`), товар
выбирался из огромного `<select>`. Переделано по тому же принципу, что
уже работает на Приходе: текстовое поле с datalist (название или SKU) +
скрытый `product_id`, резолвящийся на клиенте, кнопка «+ Добавить
позицию» без потолка (новый `webapp/static/sale-rows.js`, логика
поиска/резолва/добавления строки — то же самое, что
`purchase-rows.js`, минус вставка/фотосканирование целой накладной,
которые продаже не нужны). У каждой строки свой значок 📷 —
переиспользует уже существующий `/inventory/products/scan-label`
(тот же OpenAI vision, что у SKU-сканера товаров), распознанное
сопоставляется с каталогом сначала по SKU, потом по подстроке в
названии.

**Важное отличие от Прихода:** нераспознанная строка НЕ создаёт новый
товар автоматически — продать можно только то, что реально есть на
складе, поэтому сервер отклоняет несопоставленное название с
дружелюбной ошибкой («Не найден в каталоге: ... — выберите товар из
списка»), а не молча создаёт фантомный товар или падает. Пустой чек
(ни одной позиции) теперь тоже показывает дружелюбную ошибку вместо
молчаливого no-op редиректа, который был раньше.

Порядок формы: позиции чека → канал/гарантия → контакты клиента
(перенесены в конец, под подзаголовком) → кнопка оформления — как
попросил Павел, канал не трогал.

187/187 тестов (добавлены: строка за старым потолком в 3 слота реально
обрабатывается, несопоставленное название отклоняется, пустой чек
отклоняется). Проверено вживую на проде: создан реальный
товар/ячейка/остаток, продажа из 4 строк (0 и 3, с пропуском) прошла,
остаток списался верно (10 → 8 при qty 2), карточка продажи отрисовала
позицию — все тестовые данные (продажа, движения, остаток, ячейка,
товар) удалены после.

## Приём ремонта: фото до отправки карточки + мульти-устройство (18.08, commit `00098ba`)

Павел: карточка ремонта должна улетать в группы уже полностью
заполненной (с фото), а не текстом сразу с фото «потом отдельным
шагом»; плюс сразу принимать несколько устройств одним визитом.

**Фото на приёме, не постфактум.** Форма приёма (`repairs_list.html`)
теперь принимает фото устройства прямо на этапе заполнения
(`<input type=file>`, `enctype="multipart/form-data"`) — старый
отдельный `POST /repairs/{id}/photo` на карточке ремонта никуда не
делся (годится для замены фото позже), но `create_view` сохраняет фото
и вызывает `set_device_photo()` ДО построения и отправки карточки, так
что `notify_repair_card()` всегда уже знает о фото — карточка и фото
уходят вместе одним действием, никогда «текст сейчас, фото потом».

**Баг по пути (важный, объясняет и второй репорт Павла «фото ноутбука
не отображается в списке»):** `request.form()` отдаёт файлы как
`starlette.datastructures.UploadFile`, а НЕ `fastapi.UploadFile` — в
установленной версии FastAPI (0.115) это два несвязанных класса.
Проверка `isinstance(upload, UploadFile)` (с fastapi-классом) тихо
считала любое реально загруженное фото «файл не выбран» и никогда не
сохраняла его. Именно поэтому у ноутбука Павла фото не появилось нигде
— в БД у устройства `photo_path` был `NULL`, рендерить было нечего, это
не баг сетки карточек. Фикс: `isinstance` теперь проверяет
`starlette.datastructures.UploadFile` явно.

**Фото карточки в Telegram — sendPhoto по URL не сработал.** Первая
версия строила публичный `https://.../static/device_photos/...` URL и
слала его в Telegram (`sendPhoto` принимает URL или прямую загрузку).
Telegram ответил `400 "wrong type of the web page content"` на
абсолютно рабочий JPEG (проверено `curl`: 200, верный content-type,
валидный файл) — ненадёжное поведение конкретно против этого домена.
Переделано на прямую multipart-загрузку байтов (`files=` в `httpx`,
без URL вообще) — сразу заработало без ошибок в логах.

**«+ Добавить ещё устройство».** Один клиент с двумя устройствами за
один визит — теперь один сабмит формы создаёт отдельный `repair_orders`
на каждое устройство (тот же паттерн динамических строк, что у
Приход/Продажи — новый `webapp/static/repair-device-rows.js`, у каждой
строки своя фильтрация модели по бренду и свой 📷-сканер, без потолка
строк). Общие на весь визит — имя/телефон клиента, канал, мастер;
у каждого устройства своё — тип/бренд/модель/серийник/описание/фото/
оценка стоимости. Пустая добавленная строка молча пропускается (как в
Приход/Продаже), тронутая-но-неполная (нет типа устройства) — отклоняется
дружелюбной ошибкой, а не тихо теряется или падает в 500.

198/198 тестов (было 179; +19: мульти-устройство round-trip, отклонение
неполной строки, отклонение пустого приёма, фото на приёме появляется
сразу — без отдельного захода, невалидное фото на приёме отклоняется
без полусозданного клиента). Проверено вживую на проде: одно устройство
с реальным фото (photo_path появился сразу, отрисовался и на карточке,
и в списке, ошибок в логах после фикса upload-vs-URL не было) + два
устройства одним визитом (оба стали отдельными ремонтами). Тестовые
ремонты/устройства/клиенты и файлы фото удалены после.

**Не решено до конца:** попытка программно удалить тестовые карточки
из реальных групп «Работа»/«Мастера 007» через Bot API
(`deleteMessage`/`editMessageText`) упёрлась в необъяснённую ошибку
«message not found» несмотря на залогированную успешную отправку —
решил не гадать дальше и не трогать чат вслепую. **Стоит вручную
проверить обе группы** на предмет оставшихся тестовых карточек от
клиентов «Смоук Интейк»/«Смоук Директ Аплоад» и убрать при необходимости.

## Онбординг нового сотрудника

Входа по паролю нет вообще, поэтому нового сотрудника подключает владелец
вручную:
1. `python -m core.bootstrap <login> <password> <имя>` — создать аккаунт
   (роль передаётся внутри команды, см. файл).
2. Попросить сотрудника прислать свой Telegram ID (например, через
   `@userinfobot`).
3. `python -m core.link_telegram <login> <telegram_id>` — привязать.
4. Дальше сотрудник просто открывает бота — входит по ID, без пароля.

## Локальный запуск (разработка)

```
python3 -m venv .venv && . .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env   # CRM_SECRET_KEY, CRM_BOT_TOKEN, CRM_MINIAPP_URL, CRM_STAFF_GROUP_CHAT_ID (опционально)
export $(cat .env | xargs)
python -m core.bootstrap owner <пароль> "Имя владельца"
python -m core.link_telegram owner <telegram_id>
uvicorn webapp.main:app --reload --port 8099   # веб-часть mini app
python -m bot.bot                               # бот (меню открывает mini app)
```

## Тесты

`python simulate_tests.py` — сценарные тесты ядра (клиент→устройство→ремонт,
полный пайплайн ремонта, приход, продажи со списанием и нехваткой остатка,
telegram initData, url-токен). Работают на временной SQLite-базе во
временной директории, живую базу не трогают. Обязательны перед каждым
деплоем.

**Важно:** при ручном smoke-тесте через curl на живом сервере всегда
задавать `CRM_DB_PATH` на временный файл, либо вручную чистить тестовые
записи из `crm.sqlite3` после — один раз уже забыли и замусорили прод-базу
тестовым клиентом/товаром/ремонтом, пришлось чистить вручную.

## Деплой (Server-First, манифест п.8)

Продовая директория на BlueVPS: `/opt/electronics_crm`. Реальный процесс
пока проще, чем классический candidate/live манифеста (проект молодой,
один разработчик): rsync прямо в `/opt/electronics_crm` (с исключениями
`.venv`, `.git`, `.env`, `crm.sqlite3*`) → `simulate_tests.py` в venv на
сервере → `systemctl restart crm_web` (`crm_bot` перезапускать только если
менялся код бота) → проверка `journalctl -u crm_web -f`. Когда сотрудников
станет больше — вернуться к полноценному candidate-шагу.

systemd-юниты: `crm_web.service` (uvicorn на 127.0.0.1:8099), `crm_bot.service`
(polling). nginx + certbot на `crm.2.59.217.221.sslip.io` проксирует на
:8099 (паттерн как у `taki-verify`).

## Нельзя (манифест п.11 + специфика проекта)

- Ручной `ALTER TABLE` — только через `_ensure_column()` в `core/storage.py`.
- Хранить `CRM_SECRET_KEY` / `CRM_BOT_TOKEN` в репозитории — только в `.env`
  на сервере (в `.gitignore`).
- Списывать/перемещать товар в обход `core/inventory.record_movement()` —
  это единственный путь, который держит `stock` и `stock_movements` в
  согласованном состоянии.
- Полагаться на cookie/сессию для аутентификации в вебе — Telegram-вебвью
  их не держит между запросами, поэтому вся навигация обязана прокидывать
  `?t=` токен через `link()`/`render()`, иначе пользователя выкинет на
  повторный вход.
